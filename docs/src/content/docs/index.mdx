---
title: llmlingua-2
description: Node-native LLMLingua-2 prompt compression via @huggingface/transformers.
template: splash
hero:
  tagline: Node-native LLMLingua-2 prompt compression via @huggingface/transformers.
  actions:
    - text: Get started
      link: /llmlingua-2/guides/getting-started/
      icon: right-arrow
    - text: API reference
      link: /llmlingua-2/reference/api/
      icon: external
      variant: minimal
---

A typed, ESM-first wrapper around the [LLMLingua-2](https://github.com/microsoft/LLMLingua)
ONNX export. Runs entirely in Node (≥20) via `@huggingface/transformers`; no Python,
no native bindings. Bring-your-own peer dependency on `@huggingface/transformers@^3`.

## Three lines

```ts
import lingua from "@axiomantic/llmlingua-2";

const { compressed, reverseMap } = await lingua.compress(longText, { targetRatio: 0.5 });
const restored = await lingua.decompress(compressed, reverseMap);
```

`compress` runs the model and drops low-importance tokens. `decompress` reads the
faithful original out of `reverseMap` — see
[Decompression semantics](/llmlingua-2/guides/decompression-semantics/) for the contract.
