---
title: llmlingua-2
description: Node-native LLMLingua-2 prompt compression via @huggingface/transformers.
template: splash
hero:
  tagline: |
    Typed, dual ESM + CJS LLMLingua-2 prompt compression for Node, via @huggingface/transformers. No Python, no native bindings. Bring-your-own peer dependency on @huggingface/transformers@^4.
  actions:
    - text: Get started
      link: /llmlingua-2/0.1.0/guides/getting-started/
      icon: right-arrow
    - text: API reference
      link: /llmlingua-2/0.1.0/reference/api/
      icon: external
      variant: minimal
slug: "0.1.0"
---

## Three lines

```ts
import lingua from "@axiomantic/llmlingua-2";

const { compressed, reverseMap } = await lingua.compress(longText, { targetRatio: 0.5 });
const restored = await lingua.decompress(compressed, reverseMap);
```
